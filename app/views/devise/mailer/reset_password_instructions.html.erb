<%= render layout: 'devise/mailer/master_mail' do |f| %>
  <table>
    <thead>
      <th>Hello <%= @resource.email %>!</th>
    </thead>
    <tbody>
      <tr>
        <td>Someone has requested a link to change your password. You can do this through the link below.</td>
      </tr>
      <tr>
        <td><%= link_to 'Change my password', edit_password_url(@resource, reset_password_token: @token), class: 'button' %></td>
      </tr>
      <tr>
        <td>If you didn't request this, please ignore this email. Your password won't change until you access the link above and create a new one.</td>
      </tr>
    </tbody>
  </table>
<% end %>